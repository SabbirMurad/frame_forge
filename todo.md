1. ~~Add circle dots on the sides of container and image two control border radius~~ ✅
2. ~~add padding property to container~~ ✅
3. ~~Remove fill and stroke property form column, row, wrap, stack~~ ✅
4. ~~Add the alignment property to row and column~~ ✅
5. Make the container and frame in the layers tab closable like folder menu
6. In the typography tab each item is showing one after another, i need it to show 3 items side by side, then wrap below
7. in the model tab put the enums before the models
